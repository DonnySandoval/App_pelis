
<head>
<style>
/* Estilos para el formulario de inicio de sesión */
.login-form {
  max-width: 300px;
  margin: 0 auto;
  padding: 20px;
  background: #f0f0f0;
  border: 1px solid #ddd;
  border-radius: 5px;
}

.login-form h2 {
  font-size: 24px;
  margin-bottom: 20px;
}

.login-form .field {
  margin-bottom: 15px;
}

.login-form label {
  font-weight: bold;
}

.login-button {
  background: #007bff;
  color: #fff;
  padding: 10px 15px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}

.login-button:hover {
  background: #0056b3;
}

/* Estilos para los enlaces adicionales, si los tienes */

</style>
</head>
<h2>Iniciar Sesión</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "login-form" }) do |f| %>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Iniciar Sesión", class: "login-button" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
